create table TB_DOM_CLASSE(
	ID BIGSERIAL NOT NULL,
	DESCRICAO VARCHAR(50) NOT NULL,
	FK_FILO INTEGER REFERENCES TB_DOM_FILO(ID) NOT NULL,
	ATIVO BOOL NOT NULL,
	CONSTRAINT CLASSE_PK PRIMARY KEY (ID)
);